# Pulse Board

Console operacional (mock) para **platform / SRE**. Os numeros sao simulados; nao ha backend nem credencial neste repo.

incidentes aparecem espalhados em planilhas, Slack e tickets.

```bash
python -m http.server 4173 --directory public
```
